# 🏗️ DEEVO Intelligence Lab - Architecture

## System Overview

```
┌────────────────────────────────────────────────────────────────────────────┐
│                         DEEVO INTELLIGENCE LAB                              │
│                    Insurance Claims Decision Platform                        │
└────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────┐
                              │   Client    │
                              │  (Browser)  │
                              └──────┬──────┘
                                     │
                                     ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                            FRONTEND SERVICE                                 │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Streamlit App                                 │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐                      │  │
│  │  │   Claim    │  │    KPI     │  │ Governance │                      │  │
│  │  │  Scoring   │  │ Simulation │  │  & Audit   │                      │  │
│  │  │   Page     │  │    Page    │  │    Page    │                      │  │
│  │  └────────────┘  └────────────┘  └────────────┘                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    │                                        │
│                    FRONTEND_BACKEND_URL                                     │
└────────────────────────────────────┼───────────────────────────────────────┘
                                     │
                                     ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                            BACKEND SERVICE                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                          FastAPI App                                  │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │  │
│  │  │                         ROUTERS                                  │ │  │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │ │  │
│  │  │  │ /health │  │ /claim  │  │/decision│  │  /seed  │            │ │  │
│  │  │  │         │  │ /score  │  │   s     │  │         │            │ │  │
│  │  │  └─────────┘  └────┬────┘  └────┬────┘  └─────────┘            │ │  │
│  │  └────────────────────┼────────────┼──────────────────────────────┘ │  │
│  │                       │            │                                 │  │
│  │  ┌────────────────────▼────────────▼──────────────────────────────┐ │  │
│  │  │                    SCORING ENGINE                               │ │  │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │ │  │
│  │  │  │    Risk     │  │   Fraud     │  │  Decision   │             │ │  │
│  │  │  │ Calculator  │  │ Probability │  │   Policy    │             │ │  │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘             │ │  │
│  │  └────────────────────────────────────────────────────────────────┘ │  │
│  │                                                                       │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │                       CLIENTS                                   │  │  │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │  │
│  │  │  │   MongoDB   │  │   Ollama    │  │ Embeddings  │             │  │  │
│  │  │  │   Client    │  │   Client    │  │   Client    │             │  │  │
│  │  │  └──────┬──────┘  └─────────────┘  └─────────────┘             │  │  │
│  │  └─────────┼──────────────────────────────────────────────────────┘  │  │
│  └────────────┼─────────────────────────────────────────────────────────┘  │
└───────────────┼────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
┌───────────────┐ ┌───────────────┐
│  PostgreSQL   │ │    MongoDB    │
│               │ │               │
│  ┌─────────┐  │ │  ┌─────────┐  │
│  │decisions│  │ │  │  raw_   │  │
│  │  table  │  │ │  │ claims  │  │
│  └─────────┘  │ │  └─────────┘  │
│  ┌─────────┐  │ │               │
│  │audit_log│  │ │               │
│  │  table  │  │ │               │
│  └─────────┘  │ │               │
└───────────────┘ └───────────────┘
```

---

## Component Details

### Frontend (Streamlit)

| Component | Purpose | Technology |
|-----------|---------|------------|
| app.py | Main entry, home page | Streamlit |
| Claim Scoring | Score claims via form/JSON | Streamlit + Requests |
| KPI Simulation | ROI calculator | Streamlit + Plotly |
| Governance | Audit trail viewer | Streamlit + Pandas |

### Backend (FastAPI)

| Component | Purpose | Technology |
|-----------|---------|------------|
| main.py | App entry, middleware | FastAPI |
| routers/ | API endpoints | FastAPI Router |
| scoring/ | Risk calculation | Pure Python |
| clients/ | External services | httpx, pymongo |
| models.py | Database models | SQLAlchemy |
| schemas.py | Request/Response | Pydantic |

### Databases

| Database | Purpose | Tables/Collections |
|----------|---------|--------------------|
| PostgreSQL | Decisions & Audit | decisions, audit_log |
| MongoDB | Raw Claims | raw_claims |

---

## Data Flow

```
1. User submits claim via Frontend
         │
         ▼
2. Frontend sends POST /api/claim/score
         │
         ▼
3. Backend validates request (Pydantic)
         │
         ▼
4. Scoring Engine calculates:
   - risk_score (0-100)
   - fraud_probability (0-1)
   - decision (APPROVE/REVIEW/REJECT)
         │
         ▼
5. Generate explanation (template or Ollama)
         │
         ▼
6. Store in PostgreSQL (decisions table)
         │
         ▼
7. Create audit log entry
         │
         ▼
8. Store raw claim in MongoDB
         │
         ▼
9. Return response to Frontend
         │
         ▼
10. Display results to user
```

---

## API Endpoints

| Method | Endpoint | Purpose | Auth |
|--------|----------|---------|------|
| GET | /health | Health check | None |
| GET | / | Service info | None |
| POST | /api/claim/score | Score a claim | None |
| GET | /api/decisions | List decisions | None |
| GET | /api/decisions/{id} | Get decision detail | None |
| GET | /api/models | List models | None |
| POST | /api/seed | Seed demo data | SEED_TOKEN |

---

## Database Schema

### PostgreSQL

```sql
-- decisions table
CREATE TABLE decisions (
    decision_id UUID PRIMARY KEY,
    claim_id TEXT NOT NULL,
    customer_id TEXT NOT NULL,
    model_version TEXT DEFAULT 'rb-v1',
    policy_version TEXT DEFAULT 'policy-v1',
    risk_score REAL NOT NULL,
    fraud_probability REAL NOT NULL,
    decision TEXT NOT NULL,
    explanation TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- audit_log table
CREATE TABLE audit_log (
    id UUID PRIMARY KEY,
    decision_id UUID REFERENCES decisions(decision_id),
    event_type TEXT NOT NULL,
    event_payload JSONB NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### MongoDB

```javascript
// raw_claims collection
{
    "claim_id": "CLM-001",
    "customer_id": "CUST-001",
    "payload": {
        "amount": 5000,
        "incident_type": "collision",
        "history_score": 45
    },
    "decision_id": "uuid",
    "received_at": ISODate()
}
```

---

## Environment Variables

| Variable | Service | Purpose |
|----------|---------|--------|
| DATABASE_URL | Backend | PostgreSQL connection |
| MONGODB_URI | Backend | MongoDB connection |
| SEED_TOKEN | Backend | Protect seed endpoint |
| LOG_LEVEL | Backend | Logging verbosity |
| MODEL_VERSION | Backend | Scoring model version |
| POLICY_VERSION | Backend | Decision policy version |
| API_KEY | Backend | Optional API protection |
| FRONTEND_BACKEND_URL | Frontend | Backend API URL |

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        RAILWAY                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                    Production Environment                ││
│  │                                                          ││
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐││
│  │  │PostgreSQL│  │ MongoDB  │  │ Backend  │  │ Frontend │││
│  │  │  Plugin  │  │  Plugin  │  │ Service  │  │ Service  │││
│  │  │          │  │          │  │          │  │          │││
│  │  │ :5432    │  │ :27017   │  │ :8000    │  │ :8501    │││
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘││
│  │       │              │            │              │      ││
│  │       └──────────────┴────────────┴──────────────┘      ││
│  │                    Internal Network                      ││
│  └─────────────────────────────────────────────────────────┘│
│                              │                               │
│                    Public Domains                            │
│                              │                               │
│  ┌───────────────────────────┴───────────────────────────┐  │
│  │  backend.railway.app          frontend.railway.app    │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: Transport Security                                 │
│  └── SSL/TLS (Railway auto-configured)                      │
│                                                              │
│  Layer 2: API Security                                       │
│  └── API Key for protected endpoints                        │
│  └── SEED_TOKEN for seed endpoint                           │
│                                                              │
│  Layer 3: Application Security                               │
│  └── Input validation (Pydantic)                            │
│  └── SQL injection prevention (SQLAlchemy ORM)              │
│  └── CORS configuration                                     │
│                                                              │
│  Layer 4: Observability                                      │
│  └── Structured logging with trace_id                       │
│  └── Exception handling with audit                          │
│  └── Health check monitoring                                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Scaling Strategy

| Component | Scaling Method | Trigger |
|-----------|---------------|--------|
| Backend | Horizontal (replicas) | CPU > 70% |
| Frontend | Horizontal (replicas) | Memory > 80% |
| PostgreSQL | Vertical (upgrade plan) | Storage/connections |
| MongoDB | Vertical/Sharding | Document count |

---

*Architecture document for DEEVO Intelligence Lab*
